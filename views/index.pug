html
  include includes/header
body
  h1 Welcome to MakersBnB
  a.link-form(href='/spaces/new') Submit space for rent

  each space in spaces
    hr
    article
      div(class='spaceName')= 'Name: ' + space.name
      div(class='spaceDescription')= 'Description: ' + space.description
      div(class='spacePrice')= 'Price: £' + space.price
      div(class='availableDates')= 'Available dates: ' + space.fromDate + ' - ' + space.toDate
